#!/usr/bin/python3
from add_0 import add  # Importa solo la función add

a = 1  # Definimos la variable a en una línea
b = 2  # Definimos la variable b en otra línea

print("{} + {} = {}".format(a, b, add(a, b)))  # Imprimimos el resultado usando string format

